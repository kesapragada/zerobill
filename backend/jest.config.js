// zerobill/backend/jest.config.js
module.exports = {
  // All config is now in package.json, but this file can be kept for future overrides.
  // Or you can delete it and just keep the config in package.json.
  testEnvironment: 'node',
  verbose: true,
};